{
  "name": "react-heroku-app",
  "version": "1.0.0",
  "description": "react-heroku-app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "watch": "nodemon server.js",
    "front" : "cd front && npm start",
    "install-client" : "cd client && npm install",
    "heroku-postbuild" : "npm run install-client && npm run build",
    "build" : "cd front && npm run build"
  },
  "engines" : {
    "node" : "14.16.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
